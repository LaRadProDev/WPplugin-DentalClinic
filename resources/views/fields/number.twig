<div class="control control--number">
    {% if withLabel %}
        <label for="{{ uuid }}">{{ name }}</label>
    {% endif %}
    <input id="{{ uuid }}" type="{{ type }}" min="2019" max="2050" value="{{ value }}" name="{{ uuid }}" {{ required ? 'required' : '' }}>
</div>